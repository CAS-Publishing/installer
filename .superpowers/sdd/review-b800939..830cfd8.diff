# Review package: b800939..830cfd8

## Commits
830cfd8 feat(installer): inline-editable CAS-ID cell in Configuration (#2.1b)

## Files changed
 Editor/Wizard/Screens/SetupScreen.cs | 30 ++++++++++++++++++++++++------
 Editor/Wizard/Uss/theme.uss          |  1 +
 2 files changed, 25 insertions(+), 6 deletions(-)

## Diff
diff --git a/Editor/Wizard/Screens/SetupScreen.cs b/Editor/Wizard/Screens/SetupScreen.cs
index 61e5c48..a9b3806 100644
--- a/Editor/Wizard/Screens/SetupScreen.cs
+++ b/Editor/Wizard/Screens/SetupScreen.cs
@@ -170,26 +170,44 @@ namespace PSV.Installer.Wizard
             dot.AddToClassList("cas-dot");
             dot.AddToClassList("cas-dot--sm");
             dot.AddToClassList("cas-dot--" + tone);
             line.Add(dot);
 
             var txt = new Label(TextFor(cell));
             txt.AddToClassList("cas-setup-cell__txt");
             txt.AddToClassList("cas-status--" + tone);
             line.Add(txt);
 
-            // Each cell opens THIS platform's settings/help (CAS has separate Android/iOS assets).
-            line.AddToClassList("cas-setup-cell--link");
-            line.tooltip = cell.Req != null && cell.Req.Kind == "assetFile"
-                ? "Open the file or how to get it"
-                : "Open settings";
-            line.RegisterCallback<ClickEvent>(_ => DoCellAction(cell));
+            if (cell.Req != null && cell.Req.Editable && cell.Req.Kind == "settingsAssetField")
+            {
+                // Editable: an inline input that force-writes the managerId for this platform.
+                var input = new TextField { value = cell.Result?.Value ?? string.Empty };
+                input.AddToClassList("cas-input");
+                input.AddToClassList("cas-setup-cell__input");
+                input.RegisterCallback<FocusOutEvent>(_ =>
+                    CasIdApplier.SetManagerId(cell.Req.Platform, input.value));
+                input.RegisterCallback<KeyDownEvent>(e =>
+                {
+                    if (e.keyCode == KeyCode.Return || e.keyCode == KeyCode.KeypadEnter)
+                        CasIdApplier.SetManagerId(cell.Req.Platform, input.value);
+                });
+                line.Add(input);
+            }
+            else
+            {
+                // Read-only: each cell opens THIS platform's settings/help (CAS has separate assets).
+                line.AddToClassList("cas-setup-cell--link");
+                line.tooltip = cell.Req != null && cell.Req.Kind == "assetFile"
+                    ? "Open the file or how to get it"
+                    : "Open settings";
+                line.RegisterCallback<ClickEvent>(_ => DoCellAction(cell));
+            }
 
             return line;
         }
 
         // Opens the settings for ONE platform cell: the found file, the relevant settings asset,
         // or the help page — whichever applies to this requirement.
         private static void DoCellAction(SetupModel.Cell cell)
         {
             var req = cell.Req;
             if (req == null) return;
diff --git a/Editor/Wizard/Uss/theme.uss b/Editor/Wizard/Uss/theme.uss
index 49cda36..8ee7739 100644
--- a/Editor/Wizard/Uss/theme.uss
+++ b/Editor/Wizard/Uss/theme.uss
@@ -271,20 +271,21 @@
     background-color: #343434;
     border-bottom-width: 1px;
     border-bottom-color: #2A2A2A;
 }
 .cas-setup-row--alt { background-color: #303030; }
 .cas-setup-col-comp { flex-grow: 1.3; flex-basis: 0; flex-shrink: 1; flex-direction: row; align-items: center; }
 .cas-setup-col-plat { flex-grow: 1; flex-basis: 0; flex-shrink: 1; }
 .cas-setup-cell { flex-direction: row; align-items: flex-start; margin-bottom: 3px; }
 .cas-setup-cell__txt { font-size: 11px; white-space: normal; flex-grow: 1; margin-left: 6px; }
 .cas-setup-cell--link:hover .cas-setup-cell__txt { color: #FFFFFF; }
+.cas-setup-cell__input { margin-top: 3px; min-width: 150px; }
 .cas-muted-cell { color: #6E6E6E; font-size: 11px; }
 .cas-setup-summary { margin-top: 10px; font-size: 12px; }
 .cas-setup-summary--ok { color: #4ADE80; }
 .cas-setup-summary--warn { color: #E0A030; }
 .cas-table__head {
     flex-direction: row;
     background-color: #2C2C2C;
     padding: 6px 10px;
     border-top-width: 1px; border-bottom-width: 1px;
     border-top-color: #1F1F1F; border-bottom-color: #1F1F1F;
